<?xml version="1.0"
      encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExtendedDataEntry"
             x:Class="ExtendedDataEntry.MainPage">

	<ScrollView>
		<VerticalStackLayout
			Padding="30,0"
			Spacing="25">
			<Image
				Source="dotnet_bot.png"
				HeightRequest="185"
				Aspect="AspectFit"
				SemanticProperties.Description="dot net bot in a hovercraft number nine" />

			<Label
				Text="Extended DataEntry"
				Style="{StaticResource Headline}"
				SemanticProperties.HeadingLevel="Level1" />
			<Entry Text="This is a basic entry" />
			<BoxView HeightRequest="80" BackgroundColor="Transparent" />
			<BoxView HeightRequest="2" BackgroundColor="White" />

			<Label Text="And here is a custom entry:" />
			<local:DataEntry InitialMaxChar="4"/>

		</VerticalStackLayout>
	</ScrollView>

</ContentPage>